/* This module registered by name in index.js
Modules should take care of one thing - ie, storage, 
authentication - and be named accordingly */

const state = {
// 'Buckets' of data that msy change and be retrieved by the app

};

const getters = {
// Getters look at all the objects in state and return a subset of them

};

const mutations = {
// Simple, explicit ways we would change the data stored in state. Name mutations by
// what they do

};

const actions = {
// Functions that assemble multiple mutations to make change to records
// stored in state

};



export default { state, getters, mutations, actions };